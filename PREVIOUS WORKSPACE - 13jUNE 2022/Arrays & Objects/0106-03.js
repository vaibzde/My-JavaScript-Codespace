arr = [`a`,`b`,`c`,`h`,`z`]
let target = `c`
result = false
for(i=0; i<arr.length;  i++){
    if(arr[i]==target)
    res
    //another way of doing th aboveult = true
}
result = arr.find(e=>e===target)
console.log(result)

let obj 